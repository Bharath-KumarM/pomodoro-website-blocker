import "./PopupScreenTime.scss"

const PopupScreenTime = ({hostname})=>{

    return (
        <div className="popup-screen-time-cnt">
            {`Popup screen time for ${hostname}`}
        </div>
    )
}

export default PopupScreenTime