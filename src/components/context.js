import { createContext,  } from 'react';

export const BodyClickContext = createContext(0);

export const ContentScrollCntEleRefContext  = createContext(null)

export const LocalFavIconUrlDataContext = createContext(null)